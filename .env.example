# Supabase (auth). En producción: misma base que el API, path /supabase
# Ejemplo producción: VITE_SUPABASE_URL=http://srv-drrnn.ufro.cl/supabase
VITE_SUPABASE_URL=http://localhost:8000
VITE_SUPABASE_ANON_KEY=

# Backend API (constancias y usuarios)
# URL BASE del servidor, SIN /api/v1. El código añade /api/v1 a las rutas.
# En producción: VITE_API_URL=http://srv-drrnn.ufro.cl
# Si no la defines en producción, se usa el origen de VITE_SUPABASE_URL.
VITE_API_URL=http://localhost:3020

# Opcional
# VITE_ANON_KEY=
